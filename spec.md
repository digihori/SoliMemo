# SoliMemo 機能仕様書

> **SoliMemo = Soliloquy + Memo**  
> 自分だけにつぶやくように、思いつきを残す。

## 1. コンセプト

**自分だけにつぶやく、検索できるメモログ。**

Android では、SNS に投稿するような感覚で思いついたことを素早く記録する。記録した内容はタイムライン形式で蓄積され、後から全文検索できる。

メモデータは運営側のサーバーではなく、利用者自身の Google Drive に保存する。PC では専用アプリをインストールせず、Web ブラウザから同じデータにアクセスできる。

位置付けは次のとおり。

```text
Twitter/X
自分 → 投稿 → 他人に公開

SoliMemo
自分 → 投稿 → 自分だけに保存
```

## 2. 基本方針

システム全体は**ローカルファースト**とする。

```text
                 Google Drive
                （ユーザー所有）
                       ↑↓
             ┌─────────┴─────────┐
             │                   │
         Android              Webブラウザ
           Room               Webアプリ
             │
          ローカルDB
```

Android では、ネットワーク接続の有無にかかわらずメモを作成・閲覧できる。

Google Drive は次の役割を担う。

- クラウド保存
- バックアップ
- Android/Web 間の同期

運営者側では、メモ本文を保存するサーバーを持たない。

## 3. 対応環境

Version 1 では次の環境を対象とする。

| 環境 | 対応方針 |
| --- | --- |
| Android | Google Play で一般公開する Android アプリを提供する。 |
| PC | ブラウザベースの Web アプリを提供する。Windows、macOS、Linux、Chromebook など、ブラウザが動作する環境から利用できることを目標とする。 |

専用の Windows/macOS アプリは作らない。

## 4. Google アカウント

クラウド機能を利用するときは、Google アカウントで認証する。

Google Drive API を使用し、原則として OAuth スコープは次を使用する。

```text
https://www.googleapis.com/auth/drive.file
```

Google Drive 全体へのアクセス権限である `drive` スコープは使用しない方針とする。アプリ自身が作成・管理するファイルだけにアクセスすることで、次のように説明できる構成を目指す。

> SoliMemo はユーザーの Google Drive 全体を読みません。

## 5. メモの基本データ

メモ 1 件について、最低限次のデータを持つ。

| フィールド | 内容 |
| --- | --- |
| `id` | メモを一意に識別する ID |
| `title` | タイトル（任意） |
| `body` | 本文 |
| `createdAt` | 作成日時 |
| `updatedAt` | 更新日時 |
| `deleted` | 論理削除フラグ |

タイトルは必須にしない。例えば、次の本文だけでも投稿できる。

```text
OAuthはdrive.fileでいけそう
```

一方、長期間保存する情報には、次のようにタイトルを付けることもできる。

```text
タイトル：Android OAuthについて

本文：drive.file scopeを使えば……
```

つまり、**つぶやきとしても、従来型メモとしても使える**ようにする。

## 6. Android トップ画面

トップ画面は「メモ一覧」よりも、**自分専用タイムライン**に近い UI とする。

```text
┌─────────────────────────────┐
│ SoliMemo              🔍   │
├─────────────────────────────┤
│ 今、何を残しておく？         │
│                             │
│                       投稿  │
├─────────────────────────────┤
│ 今日                        │
│                             │
│ 19:42                       │
│ OAuthはdrive.fileで          │
│ いけそう                    │
│                             │
│ 18:20                       │
│ メモアプリの名前を考える     │
│                             │
│ 14:05                       │
│ 帰りに電池を買う             │
│                             │
└─────────────────────────────┘
```

基本は、更新日時または作成日時の新しい順に表示する。SNS 的な軽さを重視する。

## 7. 新規メモ

トップ画面から直接入力できることを基本とする。入力して投稿すると、即座にローカルへ保存する。

```text
入力
 ↓
Roomへ保存
 ↓
タイムラインへ即反映
 ↓
Google Driveへ非同期同期
```

ネットワーク通信の終了を待ってから画面へ反映する設計にはしない。

## 8. メモ編集

既存メモをタップすると編集画面を開く。編集画面では次の項目を編集できる。

- タイトル（任意）
- 本文

保存ボタンを押させる設計にはせず、原則として**自動保存**とする。

編集内容が変更されたら Room へ保存し、`dirty = true` として後から Google Drive へ同期する。

## 9. 検索

検索は、このアプリの主要機能と位置付ける。

検索対象は次のとおり。

- タイトル
- 本文

検索欄へ入力すると、リアルタイムに絞り込む。例えば「タイラバ」と入力すれば、次のようなメモが即座に表示される。

```text
タイラバのドラグ設定
走水でタイラバ
次回買うタイラバ
```

「分類をきれいにする」よりも、次の体験を重視する。

> **覚えている言葉を入れれば見つかる。**

初期版は SQLite 検索でもよいが、メモ件数の増加を考慮して SQLite FTS の採用も検討する。

## 10. フォルダ・タグ

Version 1 では実装しない方針とする。

ユーザーに分類作業を要求せず、次の流れを基本思想とする。

```text
とりあえず書く
↓
後で検索する
```

実際に利用して必要性が確認できた場合のみ、将来タグなどを追加する。

## 11. Google Drive 保存

Google Drive 上には SoliMemo 専用領域を作る。当面は次の構成を想定する。

```text
Google Drive
└── SoliMemo
    └── notes
        ├── UUID.md
        ├── UUID.md
        └── UUID.md
```

**1 メモ = 1 ファイル**とする。全データを巨大な 1 ファイルにまとめない。

これにより、Android と Web で別々のメモを編集した場合の競合を減らす。

## 12. 保存フォーマット

現時点では、Markdown ファイルを第一候補とする。

```markdown
---
id: 35c15f2a-....
title: Android OAuthについて
created: 2026-08-11T09:30:00+09:00
updated: 2026-08-11T09:45:00+09:00
---

drive.file scopeを使えば、
Google Drive全体へのアクセスは不要。
```

Markdown を採用する理由は、アプリが将来存在しなくなっても、データを人間が読める状態で残すためである。

```text
SoliMemo終了
     ↓
Google Driveには.mdが残る
     ↓
VS Code / Obsidian / テキストエディタなどで読める
```

これは SoliMemo の重要な設計思想とする。

## 13. Google Drive のファイル名

ファイル名にはタイトルを使用せず、UUID を使用する。

```text
35c15f2a-....md
```

タイトル変更時に Google Drive 上のファイル名変更が発生しないようにするためである。表示タイトルは Markdown 内部に保持する。

## 14. 同期

Android では Room を通常利用時の正本として扱い、Google Drive と同期する。

基本フローは次のとおり。

```text
Androidで編集
     ↓
Roomへ保存
     ↓
dirty = true
     ↓
Google Driveへアップロード
     ↓
dirty = false
```

アプリ起動時などには、Google Drive 側の更新も取得する。

Web から編集されたメモについては、次の条件を満たす場合に Android 側へ取得する。

```text
Drive.updatedAt > Room.updatedAt
```

## 15. 競合処理

Version 1 では、複雑なリアルタイム共同編集は実装しない。基本は **Last Write Wins** とし、更新日時が新しい方を採用する。

ただし、メモ消失事故を避けるため、将来的には次の導入を検討する。

- Google Drive のリビジョン
- 競合コピー
- 編集履歴

## 16. Web 版

Web 版では次の機能を提供する。

- Google ログイン
- メモ一覧
- タイムライン
- 検索
- 新規作成
- 閲覧
- 編集
- 削除

UI イメージは次のとおり。

```text
┌─────────────────────────────────────┐
│ SoliMemo                    🔍検索 │
├─────────────────────────────────────┤
│ 今、何を残しておく？                │
│                              [投稿] │
├─────────────────────────────────────┤
│ 2026/8/11 09:42                    │
│ OAuthはdrive.fileでいけそう         │
│                                    │
│ 2026/8/10 18:20                    │
│ アプリ名を考える                   │
│                                    │
└─────────────────────────────────────┘
```

Google Drive API へ Web ブラウザから直接アクセスする構成を本命とし、Apps Script は原則として使用しない。

## 17. 削除

メモは即座に完全削除せず、論理削除を基本とする。

```text
deleted = true
```

論理削除したメモはゴミ箱相当へ移動する。Version 1 でゴミ箱画面まで実装するかは今後決定する。一定期間後に完全削除する方式も検討する。

## 18. Version 1 で実装しない機能

機能肥大化を避けるため、次の機能は原則として初期版には含めない。

- AI
- OCR
- 手書き入力
- カレンダー
- タスク管理
- リマインダー
- 共同編集
- SNS 共有
- 他ユーザーのフォロー
- コメント
- いいね
- 独自クラウド
- 独自ユーザーアカウント
- 高度なリッチテキスト編集
- 複雑なフォルダ分類

「SNS 風」とはいっても、SNS そのものを作るわけではない。他人とはつながらない。

## 19. プライバシー方針

メモ本文は開発者側のサーバーへ送信しない。

```text
ユーザー
   ↓
Android / Web
   ↓
ユーザー自身のGoogle Drive
```

ストアの説明にも、次の特徴を明示できる設計を目指す。

> メモは運営者のサーバーではなく、あなた自身の Google Drive に保存されます。

## 20. 開発フェーズ

いきなり全体を作らず、最初に技術 PoC を行う。

### Phase 1 — Drive/OAuth PoC

Android と Web の両方から、同一の Google Drive 上にあるデータを操作できることを確認する。

```text
Androidでメモ作成
        ↓
Webで表示
        ↓
Webで編集
        ↓
Androidで変更取得
```

特に `drive.file` スコープだけで、Android OAuth クライアントと Web OAuth クライアントの間で必要なファイル共有が成立するかを検証する。ここが **GO/NO-GO ポイント**となる。

### Phase 2 — Android 基本版

次の機能を実装する。

- Room
- Compose
- メモ作成
- タイムライン
- 編集
- 削除
- 全文検索
- 自動保存

### Phase 3 — Google Drive 同期

Google 認証と Google Drive 同期を追加する。

### Phase 4 — Web 版

ブラウザから次の機能を利用できるようにする。

- 一覧
- 投稿
- 編集
- 検索

### Phase 5 — 公開

Google Play 公開に向けて、次の準備を行う。

- OAuth Production 設定
- プライバシーポリシー
- Google OAuth 関連の確認
- Play Console 登録

## 21. SoliMemo の特徴

一言でまとめると、次のようなアプリ／システムである。

> **SNS のような気軽さで自分だけに書き、必要になったら検索する。データは自分の Google Drive に残る。**

当初の「Evernote のシンプル版」から一歩進み、**自分専用タイムライン型メモ**という独自性を持つ。

仕様をいったん固定し、次は Phase 1 の技術 PoC の具体的な設計へ進む。特に `drive.file` と Android/Web 間の共有成立条件を最初に検証することで、その後の実装で大きく手戻りする可能性を減らす。
